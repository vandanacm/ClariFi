# ClariFi Client

React + TypeScript + Vite frontend for ClariFi mortgage readiness analytics.

## Setup

```bash
npm install
npm run dev
```

Requires the server running on port 8001 (see `../server/`).
